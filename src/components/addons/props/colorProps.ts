export let colorProps = {
    /* font colors */
    'base-color': {
        type: Boolean,
        default: false,
    },
    'white': {
        type: Boolean,
        default: false,
    },
    'dark': {
        type: Boolean,
        default: false,
    },
    'light-green': {
        type: Boolean,
        default: false,
    },
}