export let flexProps = {
    'flex': {
        type: Boolean,
        default: false,
    },
    'is-flex': {
        type: Boolean,
        default: false,
    },
    /* justify-content */
    'x-center': {
        type: Boolean,
        default: false,
    },
    'columns-between': {
        type: Boolean,
        default: false,
    },
    /* align-items */
    'y-center': {
        type: Boolean,
        default: false,
    },
    'columns-bottom': {
        type: Boolean,
        default: false,
    },
    'x-y-center': {
        type: Boolean,
        default: false,
    },
    'gap-sm': {
        type: Boolean,
        default: false,
    },
    'gap-xs': {
        type: Boolean,
        default: false,
    },
    'has-small-gap': {
        type: Boolean,
        default: false,
    },
}